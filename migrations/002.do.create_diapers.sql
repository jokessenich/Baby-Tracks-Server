CREATE TABLE diapers (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    diaperDate TEXT NOT NULL,
    diaperType TEXT NOT NULL,
    diaperTime TEXT NOT NULL,
    userId INTEGER REFERENCES users(id)
);